import XCTest
@testable import SwiftUI_Utils

final class SwiftUI_UtilsTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUI_Utils().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
